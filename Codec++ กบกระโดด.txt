#include <iostream>
using namespace std;
int main()
{
	float deep1=0,count=0,deep=0;
	cout << "Enter Deeping of pool : " ;
	cin >> deep1;
	deep1=deep1*100;//แปลงจากเมตรเป็นเซนติเมตร ณ ที่นี้สมมติให้ความลึก 1 ม. = 100 ซม.

	{
		for(int j=10;(j*5)-5<=deep1;)//ตรวจสอบก่อนให้วน ว่า 45 น้อยกว่าหรือเท่ากับความลึกของหลุมหรือเปล่า? (45 มาจากกบโดด 5 ครั้งแล้วจะลดลง 5 ซม.)
		{
		deep1 = deep1-45;//ถ้าใช่ให้ลบ 45 ออกจากความลึกของบ่อ แล้วแทนค่าความลึกด้วยค่าที่เหลือจากการลบ
		count=count+5;//ให้จำนวนกบที่กระโดดไปเลย 5 ครั้งก่อนเพราะรู้แน่แล้วว่าความลึกมากกว่า 45 ตอนนี้ความลึกของบ่อจะเหลือ 55 แล้วก็เอาค่าที่เหลือไปตรวจสอบอีกว่าจะวนอีกหรือเปล่า?จนกว่าความลึกจะน้อยกว่า 45
		}
		count = count + (deep1/10);//เมื่อลบความลึกของบ่อด้วย 45 เรื่อย ๆ จนความลึกน้อยกว่า 45 แล้ว ก็จะหลุดจาก loop for /เหลือเท่าไหร่ให้เอาไปหาร 10 เช่นความลึกของบ่อหลังจากลบเรื่อย  ๆ เหลือ 30 ก็ให้เอา 30/10=3 แล้วเอาไปบวกกับcount ก่อน
		//ตอนนี้ count(จำนวนครั้งที่กระโดด)จะเท่ากับ 5.5 ซึ่งต้องทำการปัดให้เป็นจำนวนเต็ม (int)
		if((float)count - (int)count > 0) 
		{
			count = count + 1;//(float)count ก็คือ 5.5 ส่วน (int) count คือ 5 [เพราะ int ไม่เอาเศษ] เอามาลบกันจะเหลือ 0.5 ก็เลยให้ตรวจสอบว่า ลบกันแล้วมากกว่า 0 หรือไม่? ถ้ามากกว่าให้ count = 5.5 + 1 จะได้เท่ากับ 6.5
			cout << "Frog have to jump   :  " << (int)count <<  endl;//เมื่อได้ 6.5 มาแล้วให้แสดงเป็นตัวแปรแบบ int จะตัดทศนิยมออกเหลือ 6 เท่านั้น^^
		}
	}
		/*
	if(deep1<500)
	{
		count = 0;
		for (int i=100;i<=deep1;)
		{
			deep1=deep1-100;
			count = count + 11;
		}
		for (int i=45;i<=deep1;)
		{
			deep1 = deep1-45;
			count = count + 5;
		}
		count = count + (deep1/10);
		cout << "Frog have to jump   :  " << (int)count <<  endl;
	}
	
	else if(deep1>=500)
	{
		count = 0;
		for(int i=10;i*50<=deep1;)
		{
			deep1=deep1-500;
			count = count + 55;
		}
		for(int i=10;i*10<=deep1;)
		{
			count = count +11;
			deep1-=100;
		}
		count = count + (deep1/10);
		cout << "Frog have to jump   :  " << (int)count <<  endl;
	}
		*/
	return(0);
}